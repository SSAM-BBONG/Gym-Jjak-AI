# Java(Spring) 조회 API 호출 클라이언트.
#
# PT추천은 결국 이 클라이언트를 쓰지 않기로 함(2026-07-22) — diet/trainer_report와
# 동일하게, Spring이 필요한 데이터(후보 PT코스, 온보딩, PT이력)를 미리 다 조회해서
# FastAPI 요청에 번들로 실어 보내는 방식으로 통일. FastAPI가 Spring에 되물어보는
# 구조는 이 코드베이스에 아직 선례가 없고, 새 내부 인증 경로가 필요해 이번엔 채택하지 않음.
#
# 이후 다른 도메인(예: 챗봇의 Function Calling)이 실제로 Spring을 되짚어 호출해야 하는
# 경우가 생기면, 그때 이 파일에 필요한 함수를 추가할 것.
